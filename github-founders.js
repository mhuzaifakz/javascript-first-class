// Founders of GitHub
const githubFounders = [
    'Tom Preston-Werner',
    'Chris Wanstrath',
    'PJ Hyett',
    'Scott Chacon'
];

// Export for use in other scripts if needed
if (typeof module !== 'undefined') {
    module.exports = githubFounders;
}
